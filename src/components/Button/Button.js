import './button.css'

const Button=({color="black", text})=>{
    // const style={
    //     color: color => {color: color} => {color}
    // }

    return <button>{text}</button>
}
// style={{color}} in button tag^
export default Button